#include <iostream>

using namespace std;

int main()
{
    int A, B, C, D, diff;

    cin >> A;
    cin >> B;
    cin >> C;
    cin >> D;

    diff = (A * B - C * D);

    cout << "DIFERENCA = " << diff << endl;

    return 0;
}
