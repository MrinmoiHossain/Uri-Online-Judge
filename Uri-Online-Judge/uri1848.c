#include <stdio.h>

int main()
{
    int sum, i;
    char name[10];

    for(i = 0; i < 3; i++){
        sum = 0;

        while(1){
            scanf("%s", name);

            if(name[0] == 'c')
                break;

            if(name[0] == '-'){

            }
        }
    }
}
